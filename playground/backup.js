// word example code for array

/* <div className="thirdEntry">
{singleItem.exm && (
  <h5>
    Example: <hr />
    {singleItem.exm
      .slice(1, singleItem.exm.length - 1)
      .split(',')
      .map((singleItem, index) => (
        <p key={index}>
          {index + 1 + '. ' + singleItem.slice(1, singleItem.length - 1)}
        </p>
      ))}
  </h5>
)}
</div> */
